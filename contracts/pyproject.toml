[project]
name = "mars-contracts"
version = "0.1.0"
description = "Shared Pydantic contracts for MARS ADMET platform (ml/, api/, frontend codegen)"
requires-python = ">=3.11"
dependencies = ["pydantic[email]>=2.6"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["mars_contracts*"]
