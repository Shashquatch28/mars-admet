"""Model-serving layer (Module 8 routing table, ml-side).

Bridges Module 4's trained artifacts to the API. Everything here is real
code exercised against real (if currently sparse) trained artifacts — no
fabricated inference. See `registry.py` for the artifact layout and
`predictor.py` for the per-molecule prediction path.
"""
